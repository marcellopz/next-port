// Hero = Primary component above the fold on the homepage that displays the hero image and call to action

import React from "react";

const Hero = () => {
  return <div className="pb-20 pt-36"></div>;
};

export default Hero;
